# Each endpoint module has to have __path attribute that specifies the endpoint path
#  as well ass __impl(data) method which implements the method and returns a tuple: (resp_code, json_data) or just
#   json_data dict (this will imply 200 response code)

